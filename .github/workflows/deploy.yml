name: SIT Deployment

on:
  workflow_dispatch:

  # Temporary trigger for GitHub UI detection
  push:
    branches:
      - sit

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: sit

    concurrency:
      group: sit-deployment
      cancel-in-progress: true

    steps:
      - name: Checkout Latest SIT Code
        uses: actions/checkout@v4
        with:
          ref: sit

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm ci

      - name: Build Application
        run: npm run build

      - name: SIT Deployment Completed
        run: echo "Latest SIT branch deployed successfully"